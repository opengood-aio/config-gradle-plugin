package io.opengood.gradle.constant

class EnvVars {
    companion object {
        const val GITHUB_TOKEN = "GITHUB_TOKEN"
        const val GITHUB_USER = "GITHUB_USER"
        const val GPG_SIGNING_PASSWORD = "GPG_SIGNING_PASSWORD"
        const val GPG_SIGNING_PRIVATE_KEY = "GPG_SIGNING_PRIVATE_KEY"
        const val OSS_REPO_PASSWORD = "OSS_REPO_PASSWORD"
        const val OSS_REPO_USERNAME = "OSS_REPO_USERNAME"
    }
}
